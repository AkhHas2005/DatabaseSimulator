# DatabaseSimulator
A Java application that simulates core database functionality - tables, schemas, tuples, and CRUD operations, etc. - using text-file persistence instead of SQL. Features a CLI interface and an object‑oriented mini DBMS architecture.
